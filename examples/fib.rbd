func fib(number) {
  let a = 0;
  let temp = 0;
  let index = 0;

  for (let b = 1; index < number; b = temp + b) {
    temp = a;
    a = b;
    index = index + 1;
  }

  return a;
}

for (let i = 0; i < 20; i = i + 1) {
  println(fib(i));
}